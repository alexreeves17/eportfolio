---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<div 
  class:list={[
    "group hover:scale-[1.02] transition-all duration-300 bg-white rounded-xl p-6 shadow-lg border border-gray-100",
    className
  ]}
>
  <slot />
</div>