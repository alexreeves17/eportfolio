---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a
  href={href}
  aria-label={label}
  class="p-3 rounded-full bg-white text-gray-700 hover:text-blue-600 hover:scale-110 transition-all duration-300 shadow-md"
>
  <slot />
</a>